import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.marketplace.capacitor.pesthresys.user',
  appName: 'Pestrehsys',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
